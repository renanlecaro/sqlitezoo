{
  "name": "sqlpark",
  "version": "1.0.1",
  "description": "",
  "main": "build.js",
  "scripts": {
    "start": "npx http-server dist  -c-1 -o & rm -rf dist/*.html && nodemon build.js --ignore dist/*.html --ignore node_modules -e js,css,html,sql --watch ",

    "build": "rm -rf dist/*.html && node build.js"
  },
  "author": "",
  "license": "ISC"
}
